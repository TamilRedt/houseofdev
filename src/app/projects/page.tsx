import PortfolioPage from "@/app/portfolio/page";
import { createMetadata } from "@/lib/seo";

export const metadata = createMetadata({
  title: "Projects",
  description:
    "Editable HouseOfDev project concepts for websites, dashboards, booking systems, and AI automations built by Tamilarasan.",
  path: "/projects",
});

export default PortfolioPage;
